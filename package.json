{
    "$schema": "https://json.schemastore.org/package.json",
    "version": "1.16.2",
    "name": "model-agent",
    "type": "module",
    "license": "MIT",
    "private": true,
    "packageManager": "bun@1.3.14",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "typecheck": "bun run typecheck --filter '*'",
        "test": "bun run test --filter '@model-agent/core'",
        "build": "bun run build --filter '*'"
    }
}